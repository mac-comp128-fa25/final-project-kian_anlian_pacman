public class FoodPelletManager {
    /*
     * TODO: Use a HashMap where each key is some tile at (x,y) and the value is 
     * the pelletShape. If PacMan traverses one of the keys, we'll call get(tile)
     * to get a foodPellet GraphicsObject and remove it from the screen. 
     */
}
